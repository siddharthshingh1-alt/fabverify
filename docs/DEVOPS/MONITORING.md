# DEVOPS: MONITORING
## CURRENT
Vercel Deployments/Functions/Logs; Supabase dashboard (Table Editor, Auth, logs); browser console (F12) for client errors.

## WHAT TO WATCH
- Failed deploys / function errors (Vercel).
- Auth failures / OTP delivery (Supabase + Twilio).
- DB errors / RLS denials.
- (Later) escrow release failures, verification API failures, anomaly flags, notification delivery.

## GROW TOWARD
Error tracking (e.g. Sentry), uptime monitoring, alerting on payment/verification failures, dashboards for key ops metrics.
